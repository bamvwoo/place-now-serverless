import { BasicButton } from "../Common/Button/BasicButton";

export default function PlaceListHeader() {
    return (
        <div>
            <BasicButton $reverse={ true }>+ 장소 추가</BasicButton>
        </div>
    )
}